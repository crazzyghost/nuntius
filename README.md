# Nuntius

Nuntius is a terminal UI (TUI) application that watches a Git repository for changes, generates AI-powered commit messages based on repo conventions, and optionally commits and pushes on the user's behalf. Built for speed, simplicity, and non-destructive defaults — it never writes (commit/push) unless explicitly configured.
